# BookHub 📚

BookHub — это REST API сервис для онлайн-библиотеки, написанный на Python с использованием FastAPI. Приложение позволяет управлять книгами, пользователями и аутентификацией.

## 🚀 Технологический стек

*   **Python** 3.11+
*   **FastAPI** — веб-фреймворк.
*   **SQLAlchemy** (AsyncIO) — ORM для работы с базой данных.
*   **PostgreSQL** — основная база данных (драйвер `asyncpg`).
*   **Alembic** — управление миграциями БД.
*   **Pydantic** — валидация данных и управление настройками.
*   **JWT & Passlib** — аутентификация и хеширование паролей.

## ✨ Функционал

### Аутентификация (`/auth`)
*   Регистрация пользователей.
*   Вход в систему (Login) — выдача JWT токена в http-only cookie.
*   Выход (Logout).
*   Получение информации о текущем пользователе (`/me`).

### Книги (`/books`)
*   **Публичный доступ:**
    *   Просмотр списка книг с пагинацией.
    *   Поиск книг по названию.
    *   Просмотр детальной информации о книге.
*   **Для Авторов и Админов:**
    *   Добавление новой книги.
    *   Редактирование своих книг (полное `PUT` и частичное `PATCH`).
    *   Удаление своих книг.
    *   Просмотр списка своих книг.

## 🛠 Установка и запуск

### Предварительные требования
*   Python 3.11 или выше
*   PostgreSQL
*   [uv](https://github.com/astral-sh/uv) (рекомендуется) или pip

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/bookhub.git
cd bookhub
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта (можно скопировать пример, если есть `.env.example`).
Заполните его вашими данными:

```ini
DB_NAME=bookhub
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres

JWT_SECRET_KEY=super_secret_key_change_me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

### 3. Установка зависимостей

С использованием `uv` (рекомендуется):
```bash
uv sync
```

Или через стандартный `pip`:
```bash
pip install .
```

### 4. Применение миграций

Создайте таблицы в базе данных с помощью Alembic:

```bash
alembic upgrade head
```

### 5. Запуск сервера

```bash
uvicorn app.main:app --reload
```

Сервер запустится по адресу: `http://127.0.0.1:8000`

## 📖 Документация API

После запуска сервера интерактивная документация доступна по адресам:

*   **Swagger UI:** [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)
*   **ReDoc:** [http://127.0.0.1:8000/redoc](http://127.0.0.1:8000/redoc)

## 📂 Структура проекта

```text
app/
├── api/            # Эндпоинты (Роутеры)
├── core/           # Конфигурация, БД, исключения
├── migrations/     # Миграции Alembic
├── models/         # SQLAlchemy модели
├── schemas/        # Pydantic схемы (DTO)
├── services/       # Бизнес-логика
└── utils/          # Утилиты
```
